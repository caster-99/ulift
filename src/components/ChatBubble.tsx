import React from "react";
import {
  ListItem,
  Avatar,
  ListItemAvatar,
  ListItemText,
  IconButton,
  Stack,
  Box,
  Typography,
  Card,
  CardActionArea,
  Grid,
} from "@mui/material";
import {} from "@mui/icons-material";
//import ChatBubble from "react-chat-bubble";

interface UserProps {
  message: string;
}

const ChatBubble = (props: UserProps): JSX.Element => {
  return <Box></Box>;
};

export default ChatBubble;
