import { Typography } from "@mui/material";
import React from "react";

const RegistroVehiculo = (): JSX.Element => {
  return <Typography>Registro de vehículos</Typography>;
};

export default RegistroVehiculo;
